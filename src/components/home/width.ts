/** Homepage-only width system (see PUBLIQUE_STYLE_GUIDE.md §8). Kept
 * separate from the sitewide `.container` utility (used by every other
 * route, incl. admin) so the homepage can run wider/full-bleed sections
 * without changing layout anywhere else. */
export const CONTENT = "mx-auto w-full max-w-[1280px] px-6 sm:px-8 lg:px-10";
export const WIDE = "mx-auto w-full max-w-[1440px] px-6 sm:px-8 lg:px-10";
